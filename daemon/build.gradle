plugins {
    id 'java'
    id 'application'
}

group 'be.flmr.secmon.daemon'
version '0.1'

dependencies {
    implementation(project(":core"))
}

application {
    mainClass = "be.flmr.secmon.daemon.DaemonApp"
}

run {
    standardInput = System.in
}